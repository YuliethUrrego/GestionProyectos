<div class="top-row ps-3 navbar navbar-dark navbar-personalizado">
    <div class="container-fluid">
        <a class="navbar-brand" href="proyectos" style="color: #000000;">Gestión de proyectos</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable navmenu-personalizado" @onclick="ToggleNavMenu">
    <nav class="flex-column nav-personalizado">
        <div class="nav-bdIndicador px-3">
            <NavLink class="nav-link nav-link-personalizado" href="usuarios" ActiveClass="nav-link-activo">
                <span class="oi oi-person" aria-hidden="true"></span> Usuarios
            </NavLink>
        </div>
        <div class="nav-bdIndicador px-3">
            <NavLink class="nav-link nav-link-personalizado" href="proyectos" ActiveClass="nav-link-activo">
                <span class="oi oi-person" aria-hidden="true"></span> Proyectos
            </NavLink>
        </div>
        <div class="nav-bdIndicador px-3">
            <NavLink class="nav-link nav-link-personalizado" href="tareas" ActiveClass="nav-link-activo">
                <span class="oi oi-person" aria-hidden="true"></span> Tareas
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
